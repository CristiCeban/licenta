import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    languageContainer: {
        flexDirection: "row",
        justifyContent: "flex-start"
    },
    flagContainer: {
        marginHorizontal: 5
    },
    flagImage: {
        width: 40,
        height: 40
    }
})